streamlit>=1.64,<2.0
PyMuPDF>=1.24.14,<2.0
pandas>=2.2,<3.0
scikit-learn>=1.4.0
rank-bm25>=0.2.2
python-docx>=1.1,<2.0

# Meaning-based search (Microsoft E5 embedding model, run locally with sentence-transformers + PyTorch).
# transformers/sentence-transformers are held to versions known to work together; newer majors need a
# newer PyTorch than Intel Macs can get.
transformers>=4.41,<4.50
sentence-transformers>=3.0,<4

# PyTorch stopped supporting Intel Macs after 2.2.2, which was built for numpy 1.x
torch==2.2.2; sys_platform == "darwin" and platform_machine == "x86_64"
numpy>=1.26,<2.0; sys_platform == "darwin" and platform_machine == "x86_64"
torch>=2.3,<3.0; sys_platform != "darwin" or platform_machine != "x86_64"
numpy>=2.0,<3.0; sys_platform != "darwin" or platform_machine != "x86_64"
